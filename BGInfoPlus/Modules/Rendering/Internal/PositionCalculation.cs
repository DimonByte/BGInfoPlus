using System;
using System.Collections.Generic;
using System.Text;

namespace BGInfoPlus.Modules.Rendering.Internal
{
    //Calcuates position of elements based on the resolution and aspect ratio of the display.
    //This is used to ensure that elements are positioned correctly regardless of the display's aspect ratio.
    //So if the base background in 1920x1080 but the user has a 800x600 display, the elements will still be positioned correctly and not be cut off or too small.
    internal class PositionCalculation
    {
    }
}
